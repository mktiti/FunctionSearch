package com.mktiti.fsearch.client.rest

/*
interface RestClient {

    companion object {
        fun forPath(basePath: String): RestClient = RestClientHolder(
                info = InfoApi(basePath),
                artifact = ArtifactApi(basePath),
                search = SearchApi(basePath)
        )
    }

    val info: InfoApi
    val artifact: ArtifactApi
    val search: SearchApi

}

private data class RestClientHolder(
        override val info: InfoApi,
        override val artifact: ArtifactApi,
        override val search: SearchApi
) : RestClient
 */